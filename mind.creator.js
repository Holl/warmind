// Game.spawns['Spawn1'].spawnCreep([WORK, CARRY, MOVE], 'Builder2', {     memory: {role: 'builder'} });


module.exports = function(spawn, role, memory){
	var currentEnergy = Game.spawns[spawn].energy;
	var MaxEnergy



	Game.spawns[spawn].spawnCreep
}